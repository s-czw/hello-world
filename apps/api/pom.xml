<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.1</version>
    <relativePath/>
  </parent>

  <groupId>app.cairn</groupId>
  <artifactId>cairn-api</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>cairn-api</name>
  <description>Cairn API — Spring Boot / Java 21</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Pin jOOQ so the codegen plugin and the managed runtime dependency are the same version. -->
    <jooq.version>3.19.15</jooq.version>
    <springdoc.version>2.7.0</springdoc.version>
    <archunit.version>1.3.0</archunit.version>
    <license-maven-plugin.version>2.4.0</license-maven-plugin.version>
    <jooq.codegen.output>${project.build.directory}/generated-sources/jooq</jooq.codegen.output>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jooq</artifactId>
    </dependency>

    <!-- Spring Data Redis / Lettuce (both Apache-2.0) — Valkey-backed login rate-limit counters. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <!-- OpenAPI / springdoc v2 (Apache-2.0) -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- Flyway (Apache-2.0) -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <!-- PostgreSQL driver (BSD-2) -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Bouncy Castle (MIT-style) — available for argon2 primitives if needed. -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>1.79</version>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Unit tests always run; @Tag("integration") tests are excluded here (see the 'integration' profile). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludedGroups>integration</excludedGroups>
        </configuration>
      </plugin>

      <!--
        License gate (D-021). `mvn license:add-third-party` emits
        target/generated-sources/license/THIRD-PARTY.txt listing every runtime dependency's
        license; scripts/check-licenses-java.sh greps it against the allowlist (GPL/AGPL/SSPL = fail,
        LGPL/EPL/MPL = warn). Scoped to compile+runtime so test-only deps don't gate a shipped build.
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <version>${license-maven-plugin.version}</version>
        <configuration>
          <outputDirectory>${project.build.directory}/generated-sources/license</outputDirectory>
          <thirdPartyFilename>THIRD-PARTY.txt</thirdPartyFilename>
          <excludedScopes>test,provided</excludedScopes>
          <includeTransitiveDependencies>true</includeTransitiveDependencies>
          <sortArtifactByName>true</sortArtifactByName>
          <!-- Normalize a few common SPDX-name variants so the allowlist grep is stable. -->
          <licenseMerges>
            <licenseMerge>Apache-2.0|Apache License, Version 2.0|The Apache Software License, Version 2.0|Apache 2.0|Apache License 2.0</licenseMerge>
            <licenseMerge>BSD-2-Clause|The BSD License|BSD License|BSD</licenseMerge>
            <licenseMerge>BSD-3-Clause|The BSD 3-Clause License|New BSD License</licenseMerge>
            <licenseMerge>MIT|The MIT License|MIT License|MIT license</licenseMerge>
            <licenseMerge>EPL-1.0|Eclipse Public License 1.0|Eclipse Public License - v 1.0</licenseMerge>
            <licenseMerge>EPL-2.0|Eclipse Public License 2.0|Eclipse Public License - v 2.0|Eclipse Public License v2.0</licenseMerge>
          </licenseMerges>
        </configuration>
      </plugin>

      <!-- jOOQ code generation from the Flyway DDL scripts (build-time, no DB required). -->
      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>
        <version>${jooq.version}</version>
        <executions>
          <execution>
            <id>jooq-codegen</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq-meta-extensions</artifactId>
            <version>${jooq.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <generator>
            <database>
              <name>org.jooq.meta.extensions.ddl.DDLDatabase</name>
              <properties>
                <property>
                  <key>scripts</key>
                  <value>src/main/resources/db/migration/*.sql</value>
                </property>
                <property>
                  <key>sort</key>
                  <value>flyway</value>
                </property>
                <property>
                  <key>defaultNameCase</key>
                  <value>lower</value>
                </property>
                <property>
                  <key>parseDialect</key>
                  <value>POSTGRES</value>
                </property>
                <property>
                  <key>unqualifiedSchema</key>
                  <value>none</value>
                </property>
              </properties>
              <includes>.*</includes>
              <excludes>flyway_schema_history</excludes>
              <!--
                jsonb columns (activity_log.diff, notifications.payload) are simulated as H2 JSON during
                DDL codegen, so jOOQ would generate a plain JSON field. Force them to jOOQ's JSONB type so
                the runtime binds `cast(? as jsonb)` against the real Postgres jsonb columns.
              -->
              <forcedTypes>
                <forcedType>
                  <name>JSONB</name>
                  <includeTypes>JSON</includeTypes>
                </forcedType>
              </forcedTypes>
            </database>
            <generate>
              <javaTimeTypes>true</javaTimeTypes>
              <deprecated>false</deprecated>
              <records>true</records>
              <pojos>false</pojos>
            </generate>
            <target>
              <packageName>app.cairn.api.jooq</packageName>
              <directory>${jooq.codegen.output}</directory>
            </target>
          </generator>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Integration tests (@Tag("integration"), JUnit + real Postgres). Activated with -Pintegration.
      Unit tests still run in the 'test' phase (surefire); failsafe runs the integration group in
      integration-test/verify. Locally: TEST_JDBC_URL=jdbc:postgresql://127.0.0.1:5432/cairn_test.
    -->
    <profile>
      <id>integration</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <configuration>
              <groups>integration</groups>
              <includes>
                <include>**/*IntegrationTest.java</include>
              </includes>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
